# One-off scripts (not auto-applied)

Place destructive or data-only SQL here (resets, backfills, ad-hoc admin
fixes). These files are documentation only — run them manually via the
Supabase SQL editor or `psql` when needed. They should never be moved into
`migrations/`.